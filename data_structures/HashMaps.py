# Dictionaries, Maps, and Hash Tables in Python -> Use built-in dict instead

# instantiate a dictionary from keylist and valuelist
d = dict(zip(keys, values))

# List of tuples
listofTuples = [("Hello", 7), ("hi", 10), ("there", 45), ("at", 23), ("this", 77)]

# Create and Initialize a dict by tuples
lexicon = dict(listofTuples)
